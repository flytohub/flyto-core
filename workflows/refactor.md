# Refactor Workflow

1. Preserve module IDs, parameter contracts, and recipe behavior unless a
   migration is documented.
2. Separate mechanical moves from assertion changes.
3. Re-run recipe bundle tests after refactoring shared execution logic.
4. Update docs if module usage changes.
