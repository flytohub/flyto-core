# Wrap-Up Workflow

1. Run `bash scripts/lint-project-memory.sh`.
2. Run relevant recipe, build, or indexer gates.
3. Update `STATE.md`, `tasks.md`, and `DECISIONS.md` for durable changes.
4. Register new handoff context in `handoffs/_registry.md`.
