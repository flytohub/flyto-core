# Planning Workflow

1. Read `STATE.md`, `ARCHITECTURE.md`, and `DECISIONS.md`.
2. Map recipe steps to modules, parameters, expected outputs, and failure modes.
3. Define how credentials and target URLs are supplied safely at runtime.
4. Decide which assertions prove the product behavior.
